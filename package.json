{
  "name": "dsh-web-companion",
  "version": "0.1.0",
  "private": true,
  "description": "DSH Web Companion — Chrome side panel that embeds the local DeepSeek Harness agent GUI and attaches pages to it",
  "type": "module",
  "scripts": {
    "graph:init": "codegraph init -i",
    "graph:sync": "codegraph sync",
    "graph:status": "codegraph status",
    "graph:query": "codegraph query",
    "graph:docs": "node scripts/doc-graph.mjs",
    "graph:check": "node scripts/doc-graph.mjs --check",
    "check:consistency": "node scripts/consistency-check.mjs",
    "check": "npm run protocol:check && npm run graph:sync && npm run graph:docs && npm run check:consistency && npm run test:protocol",
    "review:adversarial": "node scripts/pimoa-review.mjs --tool moa_verify --prompt-file scripts/review-prompts/design-adversarial.md",
    "probe:permission": "node tests/m0a/permission-probe.mjs",
    "search": "semble search",
    "probe:composer": "node tests/m0a/composer-probe.mjs",
    "probe:cookies": "node tests/m0a/cookie-matrix.mjs",
    "probe:keepalive": "node tests/m0a/keepalive-probe.mjs",
    "protocol:codegen": "node protocol/codegen.mjs",
    "protocol:check": "node protocol/codegen.mjs --check",
    "test:protocol": "node tests/protocol/contract.test.mjs",
    "check:strict": "npm run protocol:check && npm run graph:sync && npm run graph:check && npm run check:consistency && npm run test:protocol",
    "probe:attach": "node tests/m0b/attach-probe.mjs"
  },
  "devDependencies": {
    "@colbymchenry/codegraph": "^1.6.0",
    "ws": "^8.21.3"
  },
  "dsh": {
    "note": "This repository hosts three deliverables: extension/ (Chrome MV3), dsh-plugin/ (DSH cordis plugin), native-host/ (native messaging host, M2)."
  }
}
