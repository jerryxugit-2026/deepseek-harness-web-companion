# 项目台账（对照《详细设计文档》）

> 本文件绝对路径：`/Users/mac/ai_tools/dsh project/网页插件/docs/11-台账.md`
> 用途：**替代翻对话记录**。想知道"设计里说的东西，现在到底做到哪一步"，看这一份就够。
> **本台账内所有路径均为绝对路径**，可直接复制到终端或编辑器。
> 编写基准：`/Users/mac/ai_tools/dsh project/网页插件/详细设计文档.md`；git 仓库根 = `/Users/mac/ai_tools/dsh project/网页插件`，**43 次提交**，HEAD `0538a8e`（`main` 分支，**无远端**），另有 ~127 个未提交改动（v3.38 → v3.41 都在里面）。
> 最近更新：**2026-09-12 深夜（v3.41 收尾 + 一轮真机人工验收）**；那一轮的完整改动见 `/Users/mac/ai_tools/dsh project/网页插件/docs/CHANGELOG.md` 的 **v3.41 §1–§12**，验收过程见 `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/manual-acceptance-2026-09-12.md`。
> 证据一律指向仓库里的文件；标注规则：**【已验】= 有自动化证据**、**【未验】= 还没测过**、**【未做】= 没实现**、**【改了】= 与设计不同**。

---

## 0. 一句话现状

插件已经能用的完整链路：**点侧边栏 → 看到你的 DSH → 抓网页/划词/写「看左边」变成工作区文件并自动贴到输入框 → AI 能反过来读页面、点按钮、填表单、截图**。全部关键行为都有自动化脚本在真 Chrome 里跑过。

**最近状态（2026-09-12 深夜）**：`npm run check` exit 0（**20 条反模式规则 / 235+ 文件 / 23 个单测文件** / 构建 / `check:dist`）；`npm run probe:all` **8/8**；并**陪用户走完一轮真机人工验收** ——
通过：调试横幅（B4）、取消「浏览器控制」被拒（B6）、写「看左边」抓取/落盘/推送（C8 可重复 C9）、意图补投（C10）、抓取质量（D，用户判「够了」）、保留策略双向（E14）、写操作演示（无审批部署下放行且页面真变）、**ack 回执留痕**；
被实测**推翻**：B7「DevTools 打开会 attach 失败」（新版 Chrome 允许并存）；**未验**：A 授权弹窗三分支、E13 native host 自拉起、审批弹窗的交互本身。

**本台账之后的两件核心任务**（见 §10）：**A. 英文版**；**B. 可下载 / 自动检查依赖 / 一键安装部署**。

自己验证一遍（复制即可）：

```bash
cd "/Users/mac/ai_tools/dsh project/网页插件"      # 项目根（git 仓库根，绝对路径）
npm run check        # 协议一致性 + 20 条反模式规则 + 23 个单测文件 + 构建 + dist 端口门禁（不需要浏览器）
npm run probe:all    # 8 个真 Chrome/真模型测试（会自己拉起一个测试用 DSH，占 3099 端口）
```

**先定检索方式（强制）**：动手查任何东西之前，先读 `/Users/mac/ai_tools/AGENT-SEARCH-TOOLS.md`
（本机检索规范的**唯一真源**）。摘要：**文档问题走 `mcp__semble__search`，代码问题走
`mcp__codegraph__codegraph_explore`**（本项目 `projectPath: "/Users/mac/ai_tools/dsh project/网页插件"`；
查 DSH 引擎实现用 `projectPath: "/Users/mac/.codegraph-dsh-engine"`）；只有**全树字面量扫描**才用 Grep/rg，
且**在 `/Users/mac/.codegraph-dsh-engine` 里绝不要用 rg**（纯符号链接农场，rg 默认不跟随 → 恒 0 命中）。
codegraph 的返回是 **Read 等价物**（逐字源码 + 行号），不要再 Read 重开那些文件。

---

## 1. 里程碑对照（设计 §10）

| 里程碑 | 设计里要做什么 | 现在的状态 | 证据 |
|---|---|---|---|
| **M0a** 环境与能力证伪 | 只读实验：cookie 形态、第三方 cookie 屏蔽、composer 图片接口、空闲连接保活、权限手势 | **基本完成**：cookie 形态与 WS 结论已固化；空闲连接保活实测 ≥90s；权限手势结论明确 | `/Users/mac/ai_tools/dsh project/网页插件/tests/m0a/*.mjs`、`/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/probe-*.json` |
| **M0b** 最小闭环 | 侧边栏 → 登录 → 读写 DSH 输入框 → 落盘 + 一个"胶囊" | **完成**（三条硬断言全过） | `npm run probe:composer`、`npm run probe:chip` |
| **M1** 骨架与认证 | 协议单源 + 配对密钥 + 票据 + 嵌入与降级 | **完成** | `npm run test:protocol`、`npm run probe:panel` |
| **M2** 快照与注水 | 抽取网页/选区/截图、落盘、推送、胶囊、撤销、意图嗅探、自动拉起 | **主体完成**（截图入口未做，见 §5） | `npm run probe:capture`、`probe:look-left-e2e`、`probe:autostart` |
| **M3** 反向控制 | AI 能读页/点击/输入/导航/截图 + 工具注册 + 写操作受控 | **完成**，含设计未写的"运行期开关 + 逐次审批" | `npm run probe:m3-ops`、`probe:m3-control`、`probe:agent-turn` |
| **M4** 打磨交付 | 界面统一、质量回归、文档、人工验收 | **人工验收已跑过一轮**（2026-09-12：大部分项 ✅、1 条期望被推翻、3 项未验 —— 见 §5 第 2 项）；另做了设计未写的性能基线与审计工具 | `npm run probe:sites`、`probe:perf`、`audit:captures`、`/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/manual-acceptance-2026-09-12.md` |

**设计要求的"每完成一个里程碑要做 PiMoa 复评"**：设计阶段做过 4 轮对抗审核（`/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/pimoa-*.md`）；**对实现代码的复评已在 v3.40 补齐**（见 §5 第 8 项与 `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/pimoa-verification-2026-09-12.md`）。

---

## 2. 设计的量化目标（G1–G6）对照

| 目标 | 设计目标值 | 实测 | 判定 |
|---|---|---|---|
| **G1** 一键侧边栏 | 点图标 → 能打字 ≤ **2.8 秒**（**2026-09-12 用户决定下调**，原目标 1.5 秒） | **2.09 秒**（2026-09-11）。**2026-09-12 复测 p50 2743ms**（min 2585 / p95 2872，n=3）—— 这个数**不是常量**，随机器负载浮动。三段：通行证检查 2ms / 我方外壳 ~750ms / **DSH 自己启动 ~1.34–2.6s** | ✅ **按新目标达标**（原 1.5s 未达标；瓶颈不在本插件，用户已决定接受该区间，决策见 `/Users/mac/ai_tools/dsh project/网页插件/docs/CHANGELOG.md` v3.39） |
| **G2** 抓取耗时 | 轻量页 ≤300ms、标准页 ≤800ms、含截图 ≤1500ms | 轻量 **66ms**、标准 **92ms**、视口截图 ~**120ms**、整页截图 **1512ms**（加高度上限后；2026-09-12 复测 p95 **1471ms**） | ✅ 基本达标（整页原本 1865ms） |
| **G3** 本地工程闭环 | AI 能读写工作区、跑命令 | 由 DSH 本身提供；本插件不干预 | ✅（非本插件职责） |
| **G4** 免手动启动 | 不用手敲 `dsh web` | native host 自动拉起，已在你的真实 Chrome 里验过 | ✅ |
| **G5** 反向操作浏览器 | 点击/输入成功率 普通页 100%、SPA ≥80%，失败可诊断 | **【未验】**：夹具页上"点了真的变、填了真的进"已验；**主流 SPA 的成功率没测过**（需要真实站点样本） | ⚠️ 未验 |
| **G6** 轻量 | ≤1MB、零常驻进程、只走本机 | 打包 **136.7KB**（2026-09-12 实测，产物报告 `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/build-size.json`；v3.38 为 130.8KB）、无新增常驻进程、全部流量 `127.0.0.1` | ✅ |

---

## 3. 架构决策（ADR）对照：哪些照做、哪些改了

| 决策 | 设计 | 实际 | 类型 |
|---|---|---|---|
| ADR-1 DSH 单内核 | 复用 DSH 本体 | 照做 | ✅ |
| ADR-2 扩展端 **TypeScript + Vite** | 用 TS 与 Vite 构建 | **【改了】纯 JavaScript + esbuild**（`/Users/mac/ai_tools/dsh project/网页插件/extension/build.mjs`）。体积报告来自 esbuild，不是 Vite | 差异 |
| ADR-3 桥接层=DSH 进程内插件 | 插件装在 DSH 里 | 照做 | ✅ |
| ADR-4 拉起器=native messaging | Node 脚本 | 照做（`/Users/mac/ai_tools/dsh project/网页插件/native-host/*.mjs`） | ✅ |
| ADR-5 触发=意图+按钮+**快捷键** | 三种触发方式 | 按钮、输入框「看左边」、**右键菜单**都有；**【改了】快捷键未做**（Chrome 未声明 `commands`） | 差异（少一项） |
| ADR-6 视频字幕 best-effort | 尽力抓字幕 | **【未做】**：只在文件头标注"页面含视频"，没抓字幕 | 未做 |
| ADR-7 认证=/ag/enter 签发 cookie | cookie 形态 | 照做，且加了**一次性票据**（设计 v3.13 内）与四形态鉴权矩阵 | ✅ |
| ADR-9 协议单源 + codegen | 一份 schema 生成三端 | 照做（21+ 条消息、向量测试） | ✅ |
| ADR-11 「看左边」嗅探点在 DSH 侧 | 由页面内插件嗅探 | 照做 | ✅ |
| **ADR-12（v3.24 新增，设计外）** | — | **Chrome 强制**：`debugger` 不能声明为"可选权限"，必须放进必需权限；于是把"opt-in"从权限层下沉到**运行期开关 + 逐次审批**（2026-09-12 用户把这台部署改成 `approvalForWriteOps: false`：写操作不走审批缝，只由面板开关把关，`approvalMode: "off"` —— 改动见 `/Users/mac/ai_tools/dsh project/网页插件/docs/CHANGELOG.md` v3.41 §12） | 新增 |
| 桥接插件语言 | 设计文档 03 里写的是 `.ts`（`enter.ts`/`hub.ts`） | **【改了】实际全是 `.js`**（ESM），文件名也不同 | 差异（文档与实现） |
| **client 半的源与构建** | 设计 §6.1 给了 `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/src/client/{index.ts,chip.tsx,…}` + `/Users/mac/ai_tools/dsh project/网页插件/extension/build.mjs` | **【改了】两者都不存在**：client 半只有手写产物 `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/lib/client.js`（直接改它）；`/Users/mac/ai_tools/dsh project/网页插件/package.json` 的 `build:client` 指向不存在的 `/Users/mac/ai_tools/dsh project/网页插件/extension/build.mjs` | 差异，且是坑：改 client 半没有构建、没有类型检查，只能靠单测（`/Users/mac/ai_tools/dsh project/网页插件/tests/unit/client-attach.test.mjs`）钉住 |
| **一个用户两个页面半** | 设计默认"一个 DSH 页面" | 实测常态：主 GUI 标签页 **+** 侧边栏 iframe 里那份 DSH GUI，两者共享会话存储但是**两个独立 React 应用** | v3.39 前 `attach` 广播给两份 ⇒ 一次抓取建两个会话（已修，见 §8-13） |

---

## 4. 测试用例（E2E）对照

| 用例 | 状态 | 证据 |
|---|---|---|
| E2E-0 最小闭环 | ✅ | `probe:composer`、`probe:chip` |
| E2E-1 嵌入与认证 | ✅ | `probe:panel` |
| E2E-2 整页快照 | ✅ | `probe:capture` |
| E2E-3 选区快照 | ✅ | `probe:capture`（选区文件仅含选区） |
| E2E-4 截图快照 | ⚠️ **部分**：截图能力验过（视口/整页，含高度上限），**面板仍没有截图按钮**；但 v3.41 补了工具层的两道回归（`browser_screenshot` 的 output schema 与 `execute()` 真值校验），另有真机实证：整页截图落盘 2003×3420 PNG | `probe:m3-ops`、`probe:perf`、`/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/manual-acceptance-2026-09-12.md` |
| E2E-5 胶囊 | ✅ | `probe:chip`（出现/撤销/回执），且宿主已改为 DSH 插槽 |
| E2E-6 意图嗅探 | ✅ | `probe:look-left-e2e` |
| E2E-7 反向操作（读/点/输入） | ✅ | `probe:m3-ops`（**39 断言**；非可信与可信两条路径都真的改变页面。其中 8 条是 v3.41 新增的**真值驱动工具层**：用真实 CDP 值调每个工具的 `execute()`，再撞它自己声明的 output schema） |
| E2E-8 自动拉起 | ✅（真实 Chrome 验过一次） | `probe:autostart`、native host 日志 |
| E2E-9 第三方 cookie 屏蔽矩阵 | ⚠️ **未闭环**（见 §6 Q2） | `probe:cookies` |
| E2E-10 基线回归 | ⚠️ **【未验】**（本会话没跑 spike 那段） | — |
| E2E-11 意图桥接跳 | ✅ | `probe:look-left`（**13 断言**，v3.41 起含 ack 回执留痕两条） |
| E2E-12 意图全链路 | ✅ | `probe:look-left-e2e` |
| E2E-13 工具契约 | ✅ | `test:browser-tools`（**33 断言**；v3.41 起含 `browser_screenshot` 的 `execute()`，它此前被跳过 —— 漏掉的正是那个 `randomBytes` 未 import 的 ReferenceError） |
| E2E-14 抓取质量审计 | ✅ | `audit:captures` |
| E2E-15 多站点质量回归 | ✅ | `probe:sites`（五类页面 16 断言） |
| E2E-16 真模型回合（**设计外新增**） | ✅ | `probe:agent-turn`（模型真的调用 `browser_read`） |

---

## 5. 没做的 / 未验的（明确清单，不藏）

| # | 项 | 说明 | 影响 |
|---|---|---|---|
| 1 | ~~**G1 性能决策**~~（**已决定**，v3.40） | 实测 2.09–2.96s，瓶颈是 DSH 自身首屏（我方外壳 ~750ms，其余是 DSH 应用启动） | 用户 2026-09-12 决定：**接受 2.1–2.7 秒**，目标由 ≤1.5s 下调为 **≤2.8s（p50）**（三处文档已同步）；`perf` 探针把 G1 记为**基线、不参与退出码**，避免浮动指标周期性假红 |
| 2 | **人工验收（2026-09-12 已跑一轮）** | ✅ 通过：B4 调试横幅（**不是常驻**，只在调用进行中出现）、B6 取消开关被拒、C8/C9 写「看左边」抓取+落盘+推送（可重复）、C10 意图补投、D 抓取质量（用户判「够了」）、E14 保留策略双向、写操作演示（无审批部署放行 + 页面真变）、ack 回执留痕<br>❌ 被实测推翻：B7「打开 DevTools ⇒ attach 失败」（Chrome 150 允许多个调试器并存；文档已改）<br>⛔ 仍未验：A 授权弹窗三分支（用户决定不跑）、E13 native host 自拉起（日志只有 2026-09-11 那次）、审批弹窗的交互本身（本机会话策略是 `never`，弹窗弹不出来） | 全记录（含每项的证据与解锁条件）：`/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/manual-acceptance-2026-09-12.md`；清单：`/Users/mac/ai_tools/dsh project/网页插件/docs/09-manual-checklist.md` |
| 3 | **G5 成功率** | 普通页 100% 已验，**主流 SPA ≥80% 没测** | 需要真实站点样本集 |
| 4 | **快捷键** | ADR-5 提到；改为右键菜单 | 体验少一个入口 |
| 5 | **视频字幕** | ADR-6 的 best-effort 未实现 | 只标注"含视频" |
| 6 | **域名黑名单** | M4 列表里有 | 抓取范围无法按域名排除 |
| 7 | **一键安装/部署**（升级为**新核心任务 B**） | 现在的手工流程：`npm install` → `node /Users/mac/ai_tools/dsh project/网页插件/scripts/init-key.mjs` → `node /Users/mac/ai_tools/dsh project/网页插件/native-host/install.mjs` → `npm run build:ext` → `chrome://extensions` 手动加载 → **手改 `/Users/mac/.dsh/profiles/web/cordis.patch.yml` 插一行绝对路径** | 安装不算"一键"。**七条已查清的阻力点**（插件挂载绝对路径、扩展 ID 由 `manifest.key` 决定、Chrome 不允许静默装扩展、native host 清单路径、`dsh`/Node 依赖、`dist` 端口门禁、没有打包脚本）与建议的第一步见 `/Users/mac/ai_tools/dsh project/网页插件/docs/HANDOFF.md` 第二节 B |
| 8 | ~~**对实现的 PiMoa 复评**~~（v3.40 完成） | 设计阶段审过 4 轮、代码没再审 | 已补：两批分片代码审核 + 我逐条**验真**（17 条桥接层发现 → 15 真 / 2 部分真 / 0 误读；另由我核出 1 条 MAJOR：debugger 未做运行期 opt-in），证据 `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/pimoa-verification-2026-09-12.md` |
| 9 | **CI 接入** | `npm run check` 可直接进 CI；`probe:all` 需要本机 Chrome + DSH | 目前只能本地跑，见 `/Users/mac/ai_tools/dsh project/网页插件/docs/10-automation.md` |
| 10 | **抓取流程的截图入口** | 能力有，入口没有（面板无按钮、`intentCaptureMode` 默认整页） | 想要"抓页面+截图"要手动开 |
| 11 | ~~**积压抓取永远投不出去**~~（v3.38 查出，**v3.41 已修**） | `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/src/host/routes/attach.js` 在无客户端时 `store.enqueue()`（响应如实写 `deliveredTo: []`），但 client 半发的 `request-pending` **在 host 侧没有 handler**，唯一能 `drain()` 的 `GET /ag/pending` **全仓无调用者** ⇒ 页面没开时抓的东西只会留在内存队列（上限 32，超了静默丢最旧） | v3.41 修：`request-pending` 现在按 owner 绑定投递（`hub.pushClientPrimary`），**投不出去就放回队列**；审计记 `kind:'pending-replay'`（queued/delivered/requeued）。回归：`probe:attach` 第 7 节（5 条断言能咬）+ `/Users/mac/ai_tools/dsh project/网页插件/tests/unit/pending-delivery.test.mjs`（16 断言，含"一条不丢"那一支） |
| 14 | ~~**探针会把抓取写进仓库根**~~（2026-09-12 发现，**同日已修**） | `scripts/probe-all.mjs` 以 `cwd: <仓库根>` 拉起测试 DSH ⇒ 面板里那个 DSH 会话的工作目录/announce 的 `workspace` 就是仓库根 ⇒ 不钉 `target.workspace` 的抓取（实测 `probe:sites`）落到仓库内的 `网页捕获/` | **已修**：① spawn `cwd` 改为 `/Users/mac/ai_tools/dsh project/网页插件/.devhome/workspace-m0a`（实测产物改落 `/Users/mac/ai_tools/dsh project/网页插件/.devhome/workspace-m0a/网页捕获/`）；② 新增门禁 `/Users/mac/ai_tools/dsh project/网页插件/scripts/check-repo-root.mjs` 并接进 `npm run check`（造一个假污染实测 exit 1）；③ 顺带修掉两处探针默认端口撞车（3999 / 9233 → 3997 / 9235）。细节见 `/Users/mac/ai_tools/dsh project/网页插件/docs/HANDOFF.md` 避坑第 9、10 条 |
| 13 | **英文版**（**新核心任务 A**，未开工） | 产品目前全中文：`manifest` 的 `name`/`default_title`、面板与 SW 的用户文案、**`browser_*` 工具的 description（面向模型）**、README；抓取目录名 `网页捕获` 属**数据契约**（改名会让历史 `@引用` 失效） | 面积统计与三条技术路线见 `/Users/mac/ai_tools/dsh project/网页插件/docs/HANDOFF.md` 第二节 A；开工前要先问用户三件事（一份代码双语 / 目录改不改名 / 设计文档翻不翻） |
| 12 | ~~**审批策略 `never` 下写操作会"自动拒绝 + 甩锅给用户"**~~（v3.38 查出，**v3.41 已修**） | 插件 `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/src/host/approval.js` 的 `mode` 只看 `ctx.get('approval')` 存不存在、不看策略；DSH 侧 `decide()` 在 `never` 下直接 `return "rejected"` → 模型收到 `the user rejected tool "browser_click"`。开关打开后每次点击都会被拒，而**没有人被问过**，面板文案还说"会向你请求批准" | **v3.41 修**：`/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/src/host/approval.js` 新增 `policyOf()`（按**本次调用**所属会话读 `approval.effectivePolicy`；拿不到会话时退到配置默认），`mode` 多一个真实取值 `policy-never`；写调用在 `never` 下**当场拒绝并说明是策略**（不再让引擎回一句「用户拒绝了」）；面板文案按策略分支。回归：`/Users/mac/ai_tools/dsh project/网页插件/tests/unit/write-gate.test.mjs` 第 7 节（33 断言；含「会话被改回 ask 时仍要能问」与「不许甩锅给用户」） |
| 15 | **意图重试被静默吞掉 + 无面板时静默排队**（2026-09-13 实机复现；**用户决定先不改，仅记录**） | 两条"点了没反应、事后查不出为什么"的路径：① `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/lib/client.js:544` 抓到一次后 `intentArmed=false`，只有草稿**不再以「看左边」开头**才重新武装（`:539-543`）；用户没清空输入框就再输「看左边」时**静默 `return false`** —— 不发帧、不写日志、面板零提示。该状态活在 **DSH 页面 JS 闭包**里（`:552-562`，每 800ms 轮询一次），**刷新扩展重置不了**。② `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/src/host/index.js:273` 面板未连时 `store.enqueueIntent` 排队，用户同样零反馈，直到面板连上由 `onAgentConnect`（`:122-135`）补发**最后一条**（更早的静默丢弃） | 2026-09-13 01:11 那次抓到后草稿仍以「看左边」开头 ⇒ 重输被吞 ⇒ 审计日志 01:11:04→01:17:58 **零条目**（同期 `browser_tabs` 正常返回 9 个标签页 ⇒ 扩展半在线、宿主活着，断点在**宿主之前**）⇒ 用户清空输入框后 01:17:58 正常直连（**无** `intent-replay`），产物 `/Users/mac/ai_tools/dsh project/网页捕获/2026-09-13-0117-new-repository-t-9513.md`（1401 字节，`trigger: look_left`）。**修法候选**：加可见反馈（推荐，不动抓取语义）／允许同一草稿内重复触发／两者都做。**用户 2026-09-13 选定：先不改，记进待办** |
| 16 | ~~**插件依赖「链不上」时没有兜底**~~（2026-09-13 查出，**v3.46 已修**） | `/Users/mac/ai_tools/dsh project/网页插件/bootstrap/lib/layout.mjs:229` 的 `npmInstallTargets()` **全仓只有它自己的单测在调用**；`/Users/mac/ai_tools/dsh project/网页插件/bootstrap/install.mjs:342-353` 在 DSH 里找不到 `@deepseek-ai/dsh-tools` / `@deepseek-ai/dsh-credentials` / `ws` 时**只 `warn` 就继续** ⇒ 要等第 6.5 步导入自检才以"模块找不到"失败，那时已经写了一堆文件 | **v3.46 已修**：分两路 —— `ws` 下载进 `<安装目录>/.plugin-deps/` 再链（普通包，无实例同一性要求）；两个 `@deepseek-ai/*` **硬失败 exit 3**（必须与 DSH 同一模块实例）。端到端用**假 DSH** 验过两条路（缺子包 → 第 3 步当场停下且不写挂载；只缺 `ws` → 真下载并链上），单测 25 → 30 断言，放宽"可下载"集合会红 3 条。原建议：**先链、链不上再分情况**：`ws` 是普通 npm 包（没有"必须与 DSH 同一实例"的要求），可以下载进 `<安装目录>/dsh-plugin/node_modules`；两个 `@deepseek-ai/*` 是**跑在 DSH 进程内的插件**必须绑定的同一实例（多一份副本 = 两个模块实例，工具注册会分叉），缺了应当**硬失败并给出可操作信息**，不要下载第二份 |
| 17 | ~~抓取目录数量上限 `maxFiles`~~（**用户 2026-09-13 决定不做**） | `/Users/mac/ai_tools/dsh project/网页插件/dsh-plugin/src/host/retention.js` 的策略是"mtime 超过 `retentionHours`（默认 24h）才清"，**不看文件个数** ⇒ 24 小时内抓 1000 次就有 1000 个文件；用户原话是"防止文件数量爆炸" | **不做**（用户 2026-09-13 的理由，原话：「抓取 1000 遍, 可能吗? 就算抓了又怎么样, 过了 24 小时不就删除了吗?」——已有的 24h 保留已经把增长封住，再加数量上限属于 overdesign）。原判断：需要 `maxFiles` 之类"按数量留最新 N 份"。**现状确实生效**：单测 21 条 + 用真实文件名在 `/tmp` 副本上跑真函数（37.4h 的抓取与 `assets/` 截图被删、用户自己的 `我的笔记-请勿删除.md` 保留、`retentionHours: 0` → `disabled: true`）；但**线上真实清理正例为 0**（最老一份 23.6h，还差 0.4h 到期） |

---

## 6. 未决问题（设计 §12.4 的 Q1–Q10）现状

| # | 问题 | 现状 |
|---|---|---|
| Q1 | `SameSite=Strict` 为何 fetch 通、WS 不通 | 现象已实测复现；**机制未证实**（未做 net-export 抓包） |
| Q2 | 用户开启"阻止第三方 cookie"后是否仍可用 | **未闭环**：开发环境里那个开关没真正生效，需要用真实开关重跑 |
| Q3 | 图片附件接口可达性 | 部分闭环：`createDraftImages`/`draftImages`/`encodeImage` 运行时可达；`createDraftAttachments` 在该版本**不存在** |
| Q4 | `setDraft` 的 `EditRange` 语义 | 该版本 `setDraft` **只有 1 个参数**（实测）→ `EditRange` 不存在，旧表述已废弃 |
| Q5 | DSH 接口漂移 | 持续项（靠能力探测兜底） |
| Q6 | cookie 签名格式稳定性 | 持续项（每次 DSH 升级重跑 E2E-1） |
| Q7 | 无障碍树是否需先 `enable()` | **可闭环**：本会话未先 enable 也拿到了树（14 项断言） |
| Q8 | 空闲连接是否保活 | 部分闭环：文档持有的空闲连接 ≥90s 不断；更长窗口未测 |
| Q9 | 意图路径的截图模式 | **未做**（与 §5 第 10 项同源） |
| Q10 | 校验器返回值契约易误读 | **已闭环**（并加了自动拦截规则） |

---

## 7. 与设计不同的实现方式（逐条说明为什么）

| 改动 | 设计原样 | 实际做法 | 为什么 |
|---|---|---|---|
| 扩展语言与构建 | TypeScript + Vite | JavaScript + esbuild | 更快更少依赖；代价是少了类型检查，改为靠"探针 + 契约测试"兜底 |
| `debugger` 权限 | 可选权限、用时申请 | **必需权限 + 运行期开关 + 逐次审批** | Chrome 明确拒绝把它列为可选（实测报错），否则能力直接消失 |
| 「看左边」最后一跳 | 设计只写到"嗅探 + 发 intent" | 补了 `/ag/agent` 上传"抓取请求" + 未连上时排队、连上后补发 | 不补这一跳，意图到插件就断了（面板关着时也不丢） |
| 写操作开关 | 只能改配置文件（要重启 DSH） | 新增 `POST /ag/control`，面板上一个开关，**点一下即时生效** | 否则安全默认等于功能永久不可用 |
| 抓取文件清理 | 设计未提 | **每次抓取后清掉 24 小时前、本插件命名**的文件 | 你提出：目录会无限膨胀；且它同时是 AI 的输入（上下文成本） |
| 抓取噪音清理 | 设计只有"正文抽取" | 四条可独立开关的启发式（分类标签行 / 操作标签 / 尾部统计 / 占位空链接） | 真站点实测：正文区里混着界面残留 |
| 整页截图 | 设计无上限 | 超过 12000 像素高就裁剪，并如实回报"被裁剪了" | 否则超大图会被丢弃 → 工具直接失败（现在变成"带说明的成功"） |
| 胶囊（页面上的小标签） | 设计想用 DSH 插槽 | 先做了 DOM 版，v3.33 改成**插槽版**，DOM 版留作兜底 | 插槽版让样式与生命周期跟 DSH 一致 |
| 真模型端到端 | 设计列为"需人工" | 做成自动脚本（E2E-16） | 把"只有人能验"的一环变成可回归 |
| 质量审计工具 | 设计无 | `npm run audit:captures` + 抓取文件记录构建版本 | 让"质量有没有退步"有数字可看，而不是凭感觉 |

---

## 8. 本会话修掉的真缺陷（都是跑出来的，不是设计出来的）

1. **插件把工具调用发给"僵尸连接"**：连接断开时该清的那段代码被误删，导致死连接一直留着 —— 它们收得到广播心跳、却吞掉工具调用，AI 看到的是"扩展超时"（排查方向被完全带偏）。已修复 + 新增单测。
2. **校验器返回值被误读**：导致「看左边」整条链路**静默失效**（不报错、就是不动）。已修复 + 加了自动拦截规则。
3. **把"自己家的页面"当抓取目标**：面板作为普通标签页打开时，抓取必然失败并给出误导性提示。已修复（跳过自身页面）。
4. **引用（blockquote）生成非法 Markdown**：内容没丢但格式语义丢了。已修复。
5. **`Load more` 这类按钮没被清理**：真站点上残留。已修复。
6. **截图工具忽略调用者的"整页"标志**：请求视口却给整页图（慢 20 倍）。已修复。
7. **没划选却点「Attach 选区」会静默抓整页**：已改为明确拒绝。
8. **保留策略不认截图命名**：截图目录本来会无限膨胀。已修复。
9. **「新会话」被自动带上网页 attach 文档**（v3.38，用户报的现象）：client 半的 `openFreshSession()` 调了 `sessions.open()`（抢走界面）、`applyAttach()` 无条件 `setDraft(fileRef)`（预填输入框）⇒ 任何非「看左边」的抓取都会**新建会话 + 切界面 + 预填**。会话库里两个会话相隔 5ms 被创建，其中一个首条消息就以注入的 `@网页捕获/…md` 开头。已修复（改为 `offered` + 用户点「引用」才插入；单测 `/Users/mac/ai_tools/dsh project/网页插件/tests/unit/client-attach.test.mjs` 25 断言）。
10. **右键菜单抓取从来就是坏的**（v3.38）：`/Users/mac/ai_tools/dsh project/网页插件/extension/src/sw/menu.js` 发 `trigger: 'contextmenu'`，不在 `AttachRequest.trigger` 枚举里 ⇒ 每次都在 `validateAs` 被 400 挡掉，一个文件都不落盘。已修复（改 `manual`）+ 回归单测 `/Users/mac/ai_tools/dsh project/网页插件/tests/unit/menu-trigger.test.mjs`。
11. **审计日志从未实现**（v3.38）：设计 §9 要求两半都记，实测两半都没有 —— 这正是"5ms 那次抓取查不到底"的原因。已补（插件 JSONL + 扩展环形缓冲，allow-list 只记元数据）。
12. **扩展显示名一直是 `Antigravity Web Companion`**（v3.38）：设计 §6.1 早已宣布旧名废弃，manifest 从没改。已改为 `DeepSeek 浏览器插件`（扩展 ID 不变，由钉死的 `key` 决定）。
13. **一次抓取建了两个会话**（v3.39，用户实测暴露）：`/ag/attach` 用 `hub.push()` **广播**，而用户常态有**两个 DSH 页面半**在线（主 GUI 标签页 + 侧边栏 iframe 里那份 DSH GUI），两份各跑一次 `applyAttach()` ⇒ 一次抓取 = 两个会话 + 两个胶囊 + 两次草稿插入。实测 `delivered=2`、会话成对出现（相隔 3ms）；用户在 22:23 报的"新会话自带 attach 文档"就是这个（相隔 5ms，**两个会话都是插件建的** —— 我最初的解释是错的）。已修：`hub.pushClientPrimary`（只投一个；谁要的给谁 → 侧边栏 → focused → visible → 最新），并打通 `intent → requestId → capture-result → captureId → clientId` 的归属接线。
14. **一次「看左边」抓两次**（v3.39）：嗅探器旧守卫是"草稿变了就当新意图"，而第一次抓取**会改写草稿**（追加 `@文件`）⇒ 785ms（一个轮询周期）后又抓一次，输入框里堆两个内容相同的引用。已修：episode 武装/解除（关键词离开草稿或换会话才重新武装）。
15. **发布产物被烤进测试端口**（v3.39，我自己造成的真事故）：探针临时改写 `/Users/mac/ai_tools/dsh project/网页插件/extension/src/lib/dev-config.js` 时，并发的 `npm run check` 把**测试端口 3099** 烤进了 `/Users/mac/ai_tools/dsh project/网页插件/extension/dist/`（用户 Chrome 加载的目录）⇒ 面板一直往死端口拨号、`E_EXT_OFFLINE`，而看源码完全看不出问题。已修 + 防复发：`/Users/mac/ai_tools/dsh project/网页插件/scripts/check-dist-config.mjs` 门禁（产物端口/密钥必须等于源码；已实测漂移时 exit 1），`probe:all` 收尾自动重建 `dist` 并自证。
16. **`mode:'screenshot'` 抓不到图却回 `ok:true`**（v3.41，假绿）：`captureVisibleTab` 失败时 `buildCapture` 把 `{dropped:true}` 写进 body 照常投递，而 SW 只看 `sendCapture` 的结果 ⇒ 面板说「已附加」、审计记成功，**用户唯一要的那张图根本不存在**。已修：`capture.js#shotProblem()` + SW 入口判失败并说清原因（新增 `/Users/mac/ai_tools/dsh project/网页插件/tests/unit/screenshot-mode.test.mjs`，22 断言；退回旧行为 10 条红）。
17. **积压抓取队列是"只写"的**（v3.41 修，§5 第 11 项）：client 半每次连上发的 `request-pending` **宿主侧没有 handler** ⇒ 页面没开时抓的东西永远躺在内存队列里。已修 + 投不出去就放回队列（不丢数据）。
18. **413 之后的下一个请求必踩 `ECONNRESET`**（v3.41）：超限时故意不读完请求体 ⇒ socket 必须销毁，但响应头写的是 `connection: keep-alive` —— **在撒谎**。客户端复用死连接，下一条请求直接 reset（实测 `1 413 OK → 2 peek FAIL ECONNRESET`）。已修：413 显式回 `connection: close` + 探针断言。
19. **面板"读"写开关从来没成功过**（v3.41，**我 v3.40 埋的回归**）：把初始化从 POST 改成 GET 后，Chrome 对**扩展文档发的简单 GET 不带 `Origin`**，而 F2 只认精确 Origin ⇒ 一律 403 ⇒ 面板永远显示开关是关的（写本身一直能用，所以没暴露）。已修：`guard.originOk` 对称化 F4 的写法（Origin 缺失时要求 `Sec-Fetch-Site: none` + `Sec-Fetch-Mode: cors`）+ 单测 26 断言 + 真 Chrome 探针断言「面板显示态 == 插件真实态」。
20. **`browser_ax` 在真机上完全不可用**（v3.41，真机抓出）：output schema 把 CDP 的 `Accessibility.AXNode.nodeId`（**字符串**）声明成 `num` ⇒ 引擎判 `invalid output`，模型永远拿不到无障碍树。已修 + 新增门禁：`probe:m3-ops` 用**真实 CDP 值**驱动 8 个工具的 `execute()` 并撞各自声明的 schema。
21. **`browser_screenshot` 每次调用都 `ReferenceError`**（v3.41，同一次检查抓出）：v3.40 把文件名后缀改成 `randomBytes(3)` 却**只加用法没加 import**；单测恰好跳过了 screenshot 的 `execute()`（它要落盘）所以没抓到。已修 + 单测补上该路径。
22. **页面的回执帧被静默丢弃**（v3.41，同族第三例：有生产者无消费者）：client 半从 v3.38 起就在发 `{type:'ack'}`，宿主 `onClientFrame` 却没有分支 ⇒ 审计里**一整天 0 条** ack，"引用到底插进输入框没有"事后无法回答。已修 + 探针新增两条（走真实 WS 发 ack → 读审计文件确认落盘）。真机复验：每次抓取都多一条 `{"kind":"ack",…,"status":"inserted"}`。
23. **协议收紧会连带丢帧**（v3.41）：`ErrorCode` 枚举漏掉 5 个代码里真实在用的码（`E_NO_SELECTION`/`E_READONLY`/`E_TARGET_BUSY`/`E_PERMISSION`/`E_PLUGIN`），而 `error.code` 收紧成 `$ref` 后，集合外的码会让**整帧**被判非法并被宿主丢弃。已修：补枚举 + 文档 §2.6 补全 19 个码 + 新增门禁单测（扫源文件里的码字面量逐个核对）。同批还修掉校验器一个潜伏 bug：**只有 `required` 没有 `properties` 的子模式等于什么都不检查**。
24. **`pimoa-review` 的裁决永远是 `unknown`**（v3.41）：真实回答是「Markdown + 围栏 JSON receipt」，而驱动用 `JSON.parse(block.text)` 取结构化字段 ⇒ 必然抛；同时 `fetch` 会撞 undici 的 300s body 超时（实测成功耗时 189–309s，正贴墙）。已修：`/Users/mac/ai_tools/dsh project/网页插件/scripts/pimoa-result.mjs` 解析 receipt + 换 `node:http`；两个单测文件（22 + 16 断言，含本地假 MCP 端到端）。


25. **向导在 stdin 被关掉时崩栈 / `ask()` 不认 `--yes`**（v3.45，加"问你装到哪个目录"时用 pty 撞出来）：`rl.question()` 在接口已关闭时抛 `Error [ERR_USE_AFTER_CLOSE]: readline was closed`，而"提问进行中被关"时那个 promise 还可能**永不 settle**（挂死）；另外 `ask()` 原来只看 `rl` 不看 `assumeYes` ⇒ `--apply --yes` 会照样弹问句再永久等输入。已修（统一走 `askLine()` 与 `close` 赛跑；`--yes` 直接回落默认值）+ 回归 `/Users/mac/ai_tools/dsh project/网页插件/tests/unit/wizard-interaction.test.mjs` 32 → 40 断言（退回旧写法单测直接崩在 `ERR_USE_AFTER_CLOSE`）。
26. **安装器收尾谎报"硬判据没过"**（v3.45，`--apply --yes` 实测）：非交互时 `w.pause()` 返回 false ⇒ 第 11 步复检**没跑** ⇒ `health` 是空数组，而 `overallOk([])` 是 false ⇒ 一律打"还有硬判据没过（见上面的 ❌ 与 ↳ 修法）"，可上面**一条 ❌ 都没有**。已修（抽出纯函数 `finishBanner()` 把"没查"与"查了没过"分开，并给出 doctor 自查入口）+ 回归 `/Users/mac/ai_tools/dsh project/网页插件/tests/unit/install-health.test.mjs` 45 → 52 断言（退回旧写法 2 条变红）。

---

## 9. 证据索引（要看原始输出时去哪找）

| 想看什么 | 绝对路径 |
|---|---|
| 设计真源 | `/Users/mac/ai_tools/dsh project/网页插件/详细设计文档.md` |
| 每个版本的改动与原因（含被推翻的结论） | `/Users/mac/ai_tools/dsh project/网页插件/docs/CHANGELOG.md`（v3.1 → **v3.41**；v3.41 有 §1–§12） |
| 抓取探针原始结果 | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/probe-capture.json` |
| 意图桥接跳 / 全链路 / 真模型回合 | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/look-left-probe.json`、`/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/look-left-e2e-probe.json`、`.../m3-agent-turn-probe.json` |
| 反向操作 / 写操作控制面 / debugger 能力 | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/m3-ops-probe.json`、`.../m3-control-probe.json`、`.../m3-debugger-probe.json` |
| 性能基线（含未达标项与原因） | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/perf-g1-g2.json` |
| 多站点质量回归 | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/m4-site-quality.json` |
| 打包体积 | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/build-size.json` |
| 设计阶段的对抗审核 | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/pimoa-*.md`、`/Users/mac/ai_tools/dsh project/网页插件/docs/REVIEW-v3.0.md` |
| **真机人工验收记录（2026-09-12）** | `/Users/mac/ai_tools/dsh project/网页插件/docs/reviews/manual-acceptance-2026-09-12.md` |
| 跨轮进度与续跑规则 | `/Users/mac/ai_tools/dsh project/网页插件/docs/PROGRESS.md` |
| 交接提示词 + 避坑清单 | `/Users/mac/ai_tools/dsh project/网页插件/docs/HANDOFF.md` |
| 自动化分层（哪些能无人跑） | `/Users/mac/ai_tools/dsh project/网页插件/docs/10-automation.md` |
| 人工验收清单（只列机器做不了的） | `/Users/mac/ai_tools/dsh project/网页插件/docs/09-manual-checklist.md` |
| 检索工具规范（唯一真源） | `/Users/mac/ai_tools/AGENT-SEARCH-TOOLS.md` |
| **审计日志（插件侧，v3.38）** | `/Users/mac/.dsh/logs/web-companion-audit.jsonl`（JSONL；`trigger`/`sessionMode`/`delivered`/`captureId`/`status`） |
| 审计日志（扩展侧，v3.38） | `chrome.storage.local['ag-audit']`，读法 `chrome.runtime.sendMessage({kind:'audit'})`（环形缓冲 200 条，`domain` 仅主机名） |

**运行目录（探针用）**：测试 DSH 数据目录 `/Users/mac/ai_tools/dsh project/网页插件/.devhome`；
测试抓取落盘 `/Users/mac/ai_tools/dsh project/网页插件/.devhome/workspace-m0a/网页捕获`；
**你真实使用的抓取落盘** `/Users/mac/ai_tools/dsh project/网页捕获`；
真实配对密钥 `/Users/mac/.dsh/dsh-web-companion.json`；真实插件挂载行 `/Users/mac/.dsh/profiles/web/cordis.patch.yml`。

---

## 10. 下一步（按我的建议排序）

1. **★ A. 做本项目的英文版**（新核心任务）：先把"哪些面向用户、哪些面向模型、哪些是数据契约"分清楚
   （面积统计与三条技术路线见 `/Users/mac/ai_tools/dsh project/网页插件/docs/HANDOFF.md` 第二节）；设计文档要不要一起翻**先问用户**。
2. **★ B. 做成可下载 / 自动检查依赖 / 一键安装部署的程序**（新核心任务）：先幂等 `install.mjs` / `uninstall.mjs`；
   扩展如何落地（Chrome Web Store / 企业策略 / 自建启动器）单独决策。七条已查清的阻力点见 `/Users/mac/ai_tools/dsh project/网页插件/docs/HANDOFF.md` 第二节。
3. **要用户做的**（不阻塞上面两条）：按 `/Users/mac/ai_tools/dsh project/网页插件/docs/09-manual-checklist.md` 补齐**未验**项 ——
   A 授权弹窗三分支（用户已表示暂不跑）、E13 native host 自拉起（要 kill 宿主进程）、审批弹窗的交互（需换回 `ask` 部署）。
4. 收尾类：面板截图按钮、G5 的 SPA 成功率样本、域名黑名单、快捷键、把 `probe:all` 接进 CI。
5. **意图静默路径**（**用户 2026-09-13 决定先不改**，详见 §5 第 15 项）："抓到过一次后再输「看左边」被静默吞掉"与"面板未连时静默排队"目前都是**零提示** —— 要做时给可见反馈即可，抓取语义不用动。
6. ~~**依赖链不上时的兜底**~~（§5 第 16 项）：**v3.46 已修**（`ws` 下载进暂存区再链、两个 `@deepseek-ai/*` 硬失败 exit 3）。
7. ~~**抓取目录数量上限 `maxFiles`**~~（§5 第 17 项）：**用户 2026-09-13 决定不做** —— 24h 保留已把增长封住，属 overdesign。
